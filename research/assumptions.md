# Assumptions

The controlled assumption register is `docs/02-current-state-assumptions.md`. Research must not silently convert generic industry norms or vendor claims into organization-specific facts. Each new assumption receives an ID, owner, decision impact, validation method, and due date.
