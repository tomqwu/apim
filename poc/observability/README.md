# Observability backlog

The baseline enables Kong Prometheus metrics and correlation IDs. The extended PoC adds an OpenTelemetry Collector with explicit sampling, redaction, batching, retry, queue/drop monitoring, and approved exporters. Validate that telemetry failure cannot exhaust or block the request path.
