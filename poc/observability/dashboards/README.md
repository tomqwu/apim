# Dashboard acceptance

Build dashboards from versioned queries for request rate, 4xx/5xx, gateway/upstream latency, route/service/consumer, rate-limit/auth failures, connections, resource saturation, pod health, configuration identity/sync, and certificate expiry. Keep high-cardinality fields out of metric labels and restrict tenant access.
