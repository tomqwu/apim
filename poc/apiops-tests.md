# API operations tests

Validate contract lint/breaking change, route collision, missing ownership/security metadata, policy schema, secret detection, deterministic diff, ephemeral smoke, approval, immutable promotion, audit linkage, drift, rollback, and emergency-change expiry. Prove there is one authoritative writer per entity.
