# Hybrid tests

Deploy at least two separated data-plane groups. Record all data and network flows. Interrupt management connectivity for an agreed duration; test traffic, restarts, clean scale-out, configuration, telemetry, certificate/license expiry, stale-state visibility, and recovery. Repeat for each exact candidate variant.
