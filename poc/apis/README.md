# Synthetic API contracts

Six independent OpenAPI 3.0.3 documents model gateway evaluation operations. They intentionally omit real banking data, complex business logic, and production error schemas. Keep implementation behavior and contract tests synchronized when extending them.
