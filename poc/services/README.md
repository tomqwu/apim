# Synthetic services

One dependency-free mock service implements all six operations to keep the PoC focused on gateway behavior rather than microservice scaffolding. Splitting it by domain would add deployment noise without improving the baseline evaluation.
